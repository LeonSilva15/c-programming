/*
 * All headers are precompiled and reviewed code avialable and ready to use
 * Import the standard input-ouput header
 */
#include <stdio.h>

/*
 * C programs start in the main method
 * main can return any data type, but the operating system recognizes the
 * int as an indication of error numbers
 */
int main() {
    // printf: print format
    printf("Hello World!\n");

    return 0;
}
